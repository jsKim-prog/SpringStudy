--프로젝트에 ojdbc.jar 연결한 후에 진행한다.
--프로젝트 우클릭 -> Build Path -> Config build path -> libraries 탭 -> Classpqth에 삽입 -> Apply
-- Deployment Assembly -> java build path -> ojdbc8.jar 추가-> Apply
--결과 References Libraries에 ojdbc8.jar 가 보이면 성공(p.80)

